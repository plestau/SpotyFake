<form method="post" action="{{ path('app_disco_delete', {'id': disco.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ disco.id) }}">
    <button class="btn">Delete</button>
</form>
