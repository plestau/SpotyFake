{% extends 'base.html.twig' %}

{% block title %}Edit Cantante{% endblock %}

{% block body %}
{% if app.user %}
    <h1>Edit Cantante</h1>

    {{ include('cantante/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_cantante_index') }}">back to list</a>

    {{ include('cantante/_delete_form.html.twig') }}
{% else %}
    <h1>Debes estar logueado para acceder a esta página</h1>
    <a href="{{ path('app_login') }}">Login</a>
{% endif %}
{% endblock %}
