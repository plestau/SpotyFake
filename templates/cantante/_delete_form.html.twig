<form method="post" action="{{ path('app_cantante_delete', {'id': cantante.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cantante.id) }}">
    <button class="btn">Delete</button>
</form>
